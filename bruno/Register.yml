info:
  name: Register
  type: http
  seq: 6

http:
  method: POST
  url: http://127.0.0.1:8000/user/register
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
