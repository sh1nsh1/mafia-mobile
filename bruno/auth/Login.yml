info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:8080/user/login
  body:
    type: multipart-form
    data:
      - name: username
        type: text
        value: testuser1
      - name: password
        type: text
        value: testuser1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
