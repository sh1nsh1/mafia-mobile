info:
  name: Register
  type: http
  seq: 6

http:
  method: POST
  url: http://127.0.0.1:8080/user/register
  body:
    type: json
    data: |-
      {
        "username": "user3",
        "email": "user3@example.com",
        "password": "user3"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
