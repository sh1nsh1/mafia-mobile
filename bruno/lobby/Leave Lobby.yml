info:
  name: Leave Lobby
  type: http
  seq: 4

http:
  method: POST
  url: http://127.0.0.1:8080/lobbies/:lobby_id/leave
  params:
    - name: lobby_id
      value: ""
      type: path
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
