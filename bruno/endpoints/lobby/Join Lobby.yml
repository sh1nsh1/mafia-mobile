info:
  name: Join Lobby
  type: http
  seq: 3

http:
  method: POST
  url: http://127.0.0.1:8080/lobbies/:lobby_id/join
  params:
    - name: lobby_id
      value: 023be95b
      type: path
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
