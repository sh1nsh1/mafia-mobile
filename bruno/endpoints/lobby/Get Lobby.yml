info:
  name: Get Lobby
  type: http
  seq: 2

http:
  method: GET
  url: http://127.0.0.1:8080/lobbies/:lobby_id/
  params:
    - name: lobby_id
      value: ""
      type: path
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
