info:
  name: Get User Joined Lobby
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:8080/user/lobby
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
